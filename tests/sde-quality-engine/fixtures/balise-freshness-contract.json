{
  "schemaVersion": "1.0.0",
  "timeZone": "Europe/Oslo",
  "workflow": ".github/workflows/update-static-data.yml",
  "criticalCron": "7,22,37,52 4,7,15,21 * * *",
  "cycleHours": [4, 7, 15, 21],
  "attemptMinutes": [7, 22, 37, 52],
  "publicationGraceMinutes": 20,
  "sourceResponseMaxAgeSeconds": 300,
  "policy": "A static SDE dataset is current only when generated on or after the latest refresh-cycle boundary whose first scheduled attempt plus publication grace has elapsed. The allowed age is therefore derived from the real Europe/Oslo schedule, never a fixed six-hour or thirty-hour threshold.",
  "serverLastUpdateSource": "SDE_QE_SERVER_STATUS_URL when explicitly supplied; otherwise unavailable in the isolated static parity run"
}
